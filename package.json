{
  "name": "sr03",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"pnpm -C server dev\" \"pnpm -C web dev\"",
    "build": "pnpm -C web build",
    "start": "pnpm -C server start",
    "dmg": "pnpm -C desktop install && pnpm -C desktop dmg",
    "typecheck": "pnpm -C server typecheck && pnpm -C web typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22.16"
  },
  "packageManager": "pnpm@11.10.0"
}
